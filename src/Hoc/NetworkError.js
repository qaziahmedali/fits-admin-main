import React from "react";
import NetworkDetector from "./NetworkDetector";

const NetworkError = () => {
  return <div className="text-center"></div>;
};

export default NetworkDetector(NetworkError);
